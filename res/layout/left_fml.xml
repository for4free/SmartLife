<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundColor"
    android:orientation="vertical" >

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="6" >
        <ImageView 
            android:id="@+id/left_fml_txt"
            android:layout_width="85dp"
            android:layout_height="85dp"
            android:src="@drawable/ic_family_big"
            android:layout_centerInParent="true"
            />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="未发现家庭，请创建或加入家庭"
            android:textSize="15dp"
            android:layout_centerHorizontal="true"
            android:layout_below="@+id/left_fml_txt"
            android:layout_marginTop="15dp"
            />
    </RelativeLayout>

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="4" >
        
        <Button 
            android:id="@+id/left_fml_btn_create"
            android:layout_width="140dp"
            android:layout_height="60dp"
            android:text="创建家庭"
            android:textColor="@color/green"
            android:layout_centerHorizontal="true"
            android:background="@drawable/right_pen_max"
            android:layout_marginTop="30dp"
            />
        <Button 
            android:id="@+id/left_fml_btn_join"
            android:layout_width="140dp"
            android:layout_height="60dp"
            android:text="加入家庭"
            android:textColor="#ffffff"
            android:layout_below="@+id/left_fml_btn_create"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="20dp"
            android:background="@drawable/auto_dialog_btn_clk"
            />
    </RelativeLayout>

</LinearLayout>