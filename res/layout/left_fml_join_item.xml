<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/backgroundColor" >

    <LinearLayout
        android:id="@+id/left_fml_num_item_touch"
        android:layout_width="fill_parent"
        android:layout_height="60dp"
        android:layout_margin="3dp"
        android:background="@color/backgroundColor"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:orientation="horizontal" >

        <RelativeLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:gravity="center" >

            <com.example.smartlife.CircularImage
                android:id="@+id/left_fml_num_item_img_t"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:clickable="false"
                android:focusable="false"
                android:src="@drawable/people" />
        </RelativeLayout>

        <TextView
            android:id="@+id/left_fml_num_item_txt_t"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_weight="5"
            android:clickable="false"
            android:focusable="false"
            android:text="成员昵称"
            android:textColor="#000000"
            android:textSize="16sp" />

        <RelativeLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="3" >

            <Button
                android:id="@+id/left_fml_num_item_btn_delete"
                android:layout_width="40dp"
                android:layout_height="25dp"
                android:layout_centerInParent="true"
                android:background="@drawable/fml_btn"
                android:text="删除"
                android:visibility="invisible" />
            <Button
                android:id="@+id/left_fml_num_item_btn_agree"
                android:layout_width="40dp"
                android:layout_height="25dp"
                android:layout_centerInParent="true"
                android:background="@drawable/fml_btn"
                android:text="同意"
                android:textColor="@color/green"
                android:visibility="invisible" />
        </RelativeLayout>
    </LinearLayout>

</RelativeLayout>